export enum ERoutes {
  login = '/login',
  orders = '/orders',
  home = '/home',
  profile = '/profile',
}

export enum EScreens {
  index = 'index',
  login = 'login',
  orders = 'orders',
  home = 'home',
  profile = 'profile',
}